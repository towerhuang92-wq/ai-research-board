# design.md · 后续生图唯一样式源

**标准卡已锁定（2026-09-20）。** 技术评级卡以本地实现为准：`src/App.tsx` + `src/App.css` + `src/rating.ts`。后续生图、出稿、改版先读本节「标准卡」，再读十层。未获用户点头，禁止改扫读顺序、字号档、组名或组条语义。

按 `design-system-outline.md` 十层回填。色 / 字 / 距 / 框来自标准稿  
[`【BD】桌面端 2026 关键页面`](https://www.figma.com/design/keDfTtj6WVjgKzyoIz4MRh/【BD】桌面端-2026-关键页面?node-id=453-114181)  
`keDfTtj6WVjgKzyoIz4MRh` · 页 `453:114181`（关键页输出集）+ `721:684212`（2026新架构｜设计原则）。

标准稿用来抽色 / 字 / 距，不当成每张稿必须套上的窗：

| 页 | node | 只当 |
| --- | --- | --- |
| 自定义面板 | `1867:199882` | 瓦片、点阵、直角模块 |
| AI应用 | `1868:270332` | 会话列表密度 |
| 功能栏-场景AI | `1868:271975` | 右栏 320 的可选装配 |
| 自选-紧凑 | `4194:272506` | 表行、列表 |
| 市场-环球指数 | `1762:148656` | 榜单行高 |

Figma 角色落到 **MooPC Color Token · 牛牛-白肤 `41651:1`** + 文字样式 + 可变密度「紧凑」。禁止匿名 hex。  
**默认生图只出功能面**（模块 / 表 / 图 / 瓦片）。左轨、顶栏、底栏、搜索胶囊、Trade 不是本文件的锁定项；只有用户明确要「完整桌面窗」时才装配。业务不做下单。未确认前不驱动重画。

### 标准卡（已锁定 · Rating · Apple / AAPL · 1D）

功能面，无窗铬。英文。预览 `http://localhost:5176/`。

| 锁 | 值 |
| --- | --- |
| 卡 | 宽 360，垫 16，外框 0.5px `#e5e5e6`，圆角 8，白底，无阴影。卡与卡间距 16 |
| 字 | 全卡 12/16。只有结论词 Neutral 用 16 加粗。禁止 13–15、禁止 14、禁止 28+ |
| 重 | 字段名、小模块标、组结论 12 加粗。价、涨跌、票数、提示、读法 Regular |
| 距 | 只准 4 / 8 / 16 / 24。大模块 24，身份↔周期 / 仪↔构成 8，两组分项 16，组内紧邻 4。禁 10/12/14。图内字行高 ≥ 16，轴密就减刻度 |
| 线 | 表行和卡片外框都是 0.5px。身份 / 仪 / 构成 / 组头之间不画线 |
| 扫读 | 仪 → Neutral / Weak · 10/26 → Sell/Neu/Buy 构成 → Oscillators → Moving averages → 读法 |

四组（`.band + .band` = 24）：

1. **身份周期**（组内 8）  
   一行：`Rating` 弱 + `Apple · AAPL` 12 加粗 \| 价 / 涨跌 12 Regular。  
   周期 15m / 1H / 4H / 1D / 1W / 1M，高 24；选中墨色胶囊；箭头只表方向。
2. **结论**（组内 8）  
   半圆仪居中宽 192，180° 硬边环带宽 12，竖向受光，31 刻只打在读数上、首尾隐藏、白刻居中不贯穿、无圆点、无第二环。  
   碗里一组贴底弦：Neutral 16 加粗 + Weak · 10/26 12 弱，组内 2。禁止拆到仪下或仪侧。  
   构成：Sell / Neu / Buy 字段加粗、票数 Regular（7 / 10 / 9）+ 一条 2px 红/灰/绿比例条。
3. **分项**（两组上下，间距 16，禁止两列）  
   组头一行：左组名 12 加粗，右组结论 12 加粗。  
   Oscillators → Neutral（mute）。Moving averages → Buy（up）。禁止 Mom / Trend，禁止 Mix。  
   组条 2px：左高亮（组结论色）右灰。Neutral 高亮:灰 = 1:2；Buy = 2:1。不按 RSI/MACD/SMA 子行切段。  
   下整宽四列表：名 \| 值 \| 提示 \| 结论。行高 28，名 12 加粗，其余 Regular，数字右齐。本卡行：RSI (14)、MACD (12,26)、SMA (50)、SMA (200)。
4. **读法**  
   12 Regular：`Trend up, mom flat. Near 50, above 200.`  
   脚注 12 弱：`From [Rating] · 1D: Neu 10 · Buy 9 · Sell 7. ADX 13.5.`

本卡数据（生图保持一致，除非用户改数）：Neutral 7/10/9，Weak；价 315.334 −0.28%；RSI 49.335 Mid Neu；MACD −1.80 Hist+ Buy；SMA50 316.477 −0.36% Sell；SMA200 284.355 +10.9% Buy。

### 出稿纪律（生成必守）

改动前先用用户扫读验收：这处改完，他还知不知道自己在看哪一档、结论是什么、价在均线哪一侧。少灰块、少留白不能牺牲层级。看不懂或层级乱了的优化一律回退。

1. **不要出现间距过于紧凑导致视觉不美观。** 结论词坐进半圆碗，底边贴齐左右端刻的弦，与刻度内侧至少空开 8。禁止贴在刻度上，禁止悬在碗心。
14. **图内文字跟正文同一套行高。** 12px 字必须坐在 ≥16 的行里，行与行至少空 8。轴太密就隔档标注或减档，禁止用 12px 行高去塞价轴。Volume 档已踩过：16 档×12 行 = 字贴字。
7. **就近原则。** 同一判断的字段必须成组。「中性」和「共识弱」间距 2–4，一起坐进碗里。禁止一个在碗里、一个掉到仪下。相关近、无关远。
2. **尽量避免出现无意义的装饰性元素。** 斜纹底、说不清含义的色块都不要。竖线只标均线档位和现价，禁止两端一撮装饰刻度。
3. **减少使用灰色块，让页面更干净简洁，突出重点信息。** 周期选中用墨色胶囊突出焦点，不要浅灰底铺一排，也不要用细下划线冒充选中。轨道用 1–2px 线。灰只给弱文和 Neutral 票数，不当底。
4. **可适当收留白，但不得打乱信息层级。** 不准为了撑满把结论或票数塞到仪侧面。扫读必须仍是：仪 → 结论词 / 共识 → 卖中买构成 → 分项（Oscillators / Moving averages）→ 读法。
5. **排版要成系统。** 左右齐栏；仪是居中辅图不是海报。同类模块同一骨架。禁止整卡均分空隙、禁止同一语义拆成互不相关的散件。
6. **图服从字。** 结论类模块里半圆仪宽 160–200，不要超过栏宽 40%。大于 220 即海报，会压过 16px 结论词。不准为了「有图」把仪做大。
8. **间距只准 4 的倍数：4 / 8 / 16 / 24。** 4 = 组内紧邻。8 = 同组子模块。16 = 两组分项。24 = 大模块之间。禁止 10、12、14。控件内缝可以 2。
9. **除表格外，层级只靠留白。** 四组用 24 留白切开。分割线只给表行 0.5px。卡片外框 0.5px。禁止身份 / 仪 / 构成再画分割线。
10. **同一事实只出现一次。** SMA 偏离、分组结论、票数不能既在计量条又在表里各写一遍。组条只表达组结论，不重写子行。后续作图先查有没有重复字段，再画。
11. **组条必须和右侧组结论同色同意。** 本卡 Oscillators = Neutral，Moving averages = Buy。左高亮右灰。禁止再写 Mix / Mom / Trend。禁止按子行涨跌切段。表里子行可以和组结论不同向。
12. **不要加额外编码信息。** 只改表现，不另加共识环、第二套分数、Strong buy。
13. **禁止负向优化。** 改完必须还能一眼读出周期焦点、结论、价在 50/200 哪一侧。看不懂或层级乱了就回退。
15. **新功能卡跟投资决策走，不默认用表。** 先写验收句：现在要不要动、动谁。先答能指向下一笔动作的信息（高影响事件、过权贡献），再铺归档细节。先判数据关系再选题：占比用组条，时间流用故事列表，两数对照用水平轨+刻度，只有多字段对齐扫读才用表。结论副文白话。读法解释缺口并落到动作，可折 2 行，禁止「…」。框架橙不上条。

---

## L0 界面类型（已确认；不锁壳）

- [x] 主界面：金融产品桌面端。锁的是产品类型和色 / 字 / 距，不是窗口铬件，也不是某一张栏目的场景稿
- [x] 不做：品牌宣传网站、首页大图、摄影、页脚；也不默认复刻左轨 + 顶栏 + 底栏
- [x] 气质一句话已确认
- [x] 3 个「不像」已确认
- [x] 疏密：紧凑（金融桌面默认 · 可变密度 mode「紧凑」）
- [x] 中文优先：产品默认中文；**本标准卡锁英文**
- [x] 明暗：浅色主题（牛牛-白肤）。库缺深色组件，整页不要深色
- [x] 品牌主色只给导航选中、主操作，不进涨跌和贵贱。没有壳就不画 Trade

---

## L1 工作台气质

只写判断。色值在 L3。

| 字段 | 填写 |
| --- | --- |
| 一句话 | 浅色高密度终端。数字密、标题弱。橙色只给主操作和导航选中，没有壳就不上橙胶囊。 |
| 像 | 1. 2026「自定义面板」点阵上的直角瓦片  2. 自选 + 行情三栏  3. 「功能栏-场景AI」右 320 栏 |
| 不像 | 1. 不像宣传海报  2. 不像多张一样大的卡片铺满一屏  3. 不像深色聊天应用 / 近黑仪器台 |
| 界面靠什么传递信息 | 数字 / 表格 / 图表。不靠照片、插画、超大标题 |
| 圆角规则 | 表和模块直角；选中 / 输入 / 按钮 4；搜索或胶囊按钮才两端全圆 |
| 一年后检查 | 还是同一张投研桌。只允许改密度或模块，不准整页换皮 |

标志性视觉：无。靠疏密、等宽数字和 1px 线识别。不上渐变、玻璃或海报大字。

---

## L2 业务含义色与数字规则

本层先于品牌主色。颜色表示含义，不是装饰。

### 2.1 三种颜色各管各的

| 种类 | 表示什么 | 不准表示什么 | 落到 |
| --- | --- | --- | --- |
| 涨 / 跌 | 价格和变动方向 | 贵贱、成功失败、品牌装饰 | `ct_text_trends_green` / `ct_text_trends_red` |
| 贵 / 中 / 便宜 | 估值相对历史或同行（>75 贵，<25 便宜） | 价格涨了就标绿；越低越贵的指标按贵贱上色 | 贵用跌、便宜用涨，不另开彩虹 |
| 延迟 / 冻结 / 无权限 | 数据不可用或不是实时 | 看起来像实时行情 | 标「延时」；价可冻。标准稿未抽出独立延迟色，禁止自造第四色 |
| 成功 / 警告 / 危险 | 操作有没有做成 | 涨跌 | 错误文案用跌色，不大红铺底 |
| 框架强调色 | 导航选中、主操作（有壳才画 Trade） | 盈亏、涨跌幅、估值结论、图表线条 | `ct_text_highlight` / `ct_bg_highlight` |

全产品涨跌色一致。没有同行或历史对比时，数字不要上颜色。

### 2.2 数字怎么写

| 规则 | 约定 |
| --- | --- |
| 字体 | 价格、涨跌幅、倍数用等宽数字；挂 `Body/12 Bold` 或 `NS Headline/18 Bold` |
| 对齐 | 标签左、数字右 |
| 精度 | 倍数 1 位（23.5x）。价格按市场惯例。不要写 23.4567x |
| 空值 | 「—」。不拿邻格填洞 |
| 预测 vs 历史 | 必须能区分（虚线 / 透明度），不能画成同一种实线 |
| 参照 | 分位、同行中位必须能同屏扫到。缺同行就删这一行，不删整张表 |
| 延迟 | 必须标「延时」或冻结，不能长得像实时 |

### 2.3 三层信息

1. 默认：结论（低估 / 合理 / 高估；健康 / 警告）
2. 点开：依据（P/E 15x，历史 20% 分位）
3. 再进：原始表或公式

禁止把 50 个指标一次性铺平。Ask 引用必须先写「引自 [列名]」。

---

## L3 中性底 + 框架强调色

大约 90% 的像素是灰、白和线。强调色很少用。  
组件只引用有含义的名字，不直接涂基础色值。

### 3.1 基础色值（不直接涂在组件上）

来源：2026-09-18 对标准稿绑定变量的实读 + 原则页本地色集 `VariableCollectionId:185:61937`。

| 名称 | 色值 | 备注 |
| --- | --- | --- |
| 涨 | `#00a870` | 只给方向 |
| 跌 | `#f03a55` | 只给方向 |
| 贵 / 便宜 | `#f03a55` / `#00a870` | 与跌 / 涨共用，规则走 L2 |
| 延迟 / 警告 | — | 标准稿未抽出独立 token。用次文 +「延时」字样，禁止自造琥珀 |
| 灰 50 | `#f9f9fa` | 画布 / 点阵 |
| 灰 100 | `#eff2f6` | 浅底；有导航才当轨底 |
| 灰 200 | `#e5e5e6` | 线 |
| 字 1 | `#171718` | 主 |
| 字 2 | `#646466` | 次 |
| 字 3 | `#98989a` | 弱 |
| 框架强调色 | `#f36f12` | 只给导航选中、主操作。按下 / 悬停同色相派生，不新开一种色 |
| 买入底 / 卖出底 | 涨实底 / 跌实底 | 绿实底 / 红实底，不是框架强调色 |

### 3.2 有含义的颜色名（对应 Figma 颜色变量）

| 名称 | 引用上面的基础色 | 浅色 | Figma 变量 |
| --- | --- | --- | --- |
| 画布底 | 灰 50 | `#f9f9fa` | `块 Block \| 背景/ct_bg_frame` |
| 卡片 / 表底 | 白 | `#ffffff` | `ct_bg_primary` |
| 导航底 | 灰 100 | `#eff2f6` | 新版颜色「导航背景色」 |
| 主文字 | 字 1 | `#171718` | `字色 Text/ct_text_h1` |
| 次文字 | 字 2 | `#646466` | `ct_text_h2` |
| 弱文字 | 字 3 | `#98989a` | `ct_text_h3` |
| 分割线 | 灰 200 | `#e5e5e6` | `线条 Line/ct_line_list` / separator |
| 选中描边 | 字 1 | `#171718` | 列表选中 1px 深线框，不是大色块 |
| 涨 / 跌 | 涨 / 跌 | `#00a870` / `#f03a55` | `ct_text_trends_green` / `ct_text_trends_red` |
| 框架强调色 | 橙 | `#f36f12` | `ct_text_highlight` / `ct_bg_highlight` |
| 强调色上的字 | 白 | `#ffffff` | 主操作实心底上的字 |
| 选中图标浅底 | 橙 @ 15% | `#f36f12` 15% | 有导航时才用，不新造第三色 |
| 搜索底 | 浅蓝灰半透 | `#cad2e040` | `ct_bg_tertiary` |
| 图表色 1…n | — | 未抽出独立序列 | **不得和涨跌相同**。价本身的主线可用涨绿 |

正文和背景对比度至少 4.5:1（字 1 `#171718` on 白 / 灰 50 过线；字 3 只给坐标轴和脚注）。  
同一屏里，框架强调色的实心底最多 1 处。涨跌色不算品牌色。

### 3.3 浅色 / 紧凑

| 切换 | 何时用 | 缺设计库时 |
| --- | --- | --- |
| 浅色 | 默认。mode `牛牛-白肤` `41651:1` | — |
| 深色 | 本大纲不上 | 列表 / 输入框无完整黑肤 → 整页浅色，禁止深浅混用 |
| 紧凑 | 桌面终端、表格。可变密度「紧凑」 | — |
| 常规 | 设置页、空状态，可松一档 | — |

---

## L4 字与数

每一档必须能变成一条 Figma 文字样式。画稿时禁止手写字号。  
软件界面不用超大标题（28px 以上的展示字）。  
中文界面只挂 MooPC 文字样式（字族走库 **BR Hendrix** token）。禁止把 Inter / Roboto / SF / PingFang / Noto 当中文默认。

桌面正文锁定 **12 / 16**。其他档只从 12 往上加，不加到海报字。

| 样式名 | 字体 | 字号 | 行高 | 字重 | 数字等宽 | Figma 文字样式 |
| --- | --- | --- | --- | --- | --- | --- |
| 正文 / 表 / 值 / 脚注 / 周期 | BR Hendrix token | **12** | 16 | Regular | 表内数字要 | `Text 文字/Body/12 Regular` |
| 字段名 / 小模块标 | 同上 | 12 | 16 | Bold | 否 | `Body/12 Bold` |
| 全卡最重要结论（最多 1 处） | 同上 | **16** | 20 | Bold | 否 | 挂 16，禁止 14、禁止 28+ |
| 注释 / 坐标轴 | 同上 | 12 | 16 | Regular | 否 | `Body/12 Regular`；轴可 10，禁止 9 |

规则：

- 正文字号就是 12。Oscillators / Moving averages、报价、构成数、读法、注释都是 12。
- 图内价轴 / 刻度也是 12 / 16。行高不够 16 就不写字，改减刻度。禁止 12px 槽里塞 12px 字。
- 全卡只有最重要的结论词用 16，不再用 14，不再用 28–34。
- 只加粗字段名、小模块标和组结论（Neutral / Buy）。数字、涨跌、提示、读法里的判断都是 Regular。
- 字重最多 2 档：Regular + Bold。
- 禁止为了「好看」把正文抬到 13–15，导致一屏信息变少。

---

## L5 疏密 / 表与模块（壳不锁）

### 5.1 画板与模块，不锁窗口铬件

本层锁的是模块内部尺寸：表行、输入、Tab、线。  
**不锁** 1440×900、左轨 48、顶栏 44、底栏 32、搜索胶囊、Trade。

生图默认：

- 只画这次要的功能面（一块瓦、一张表、一栏报价都可以）
- 画板按内容 Hug 或按用户指定尺寸，禁止为了「像桌面」自动加铬件
- 禁止把 `主窗口框体` 当成每张稿的外框
- 等大卡墙仍然失败；失败原因是信息结构，不是「没套壳」

只有用户明确说「完整桌面窗 / 套主窗口」时，才引用标准稿装配（原则页 `721:684418`）：收起轨 48、顶栏 44、底栏 32、右栏 320。原则页另有 1260 / 1760 变宽样张，也不当默认锁。

内容控件（有壳无壳都用）：

| 区 | 尺寸 | 备注 |
| --- | --- | --- |
| 表行高 | **28** | 各国市场榜单、表单行。≠ 自选元件 46 |
| 自选列表行 | **220×46** | 仅自选列表用 |
| 一级选项卡 | 高 36 | 下划线 Tab |
| 二级选项卡 | 高 40 / 36 | 黑胶囊 |
| 三级选项卡 | 高 36 | 浅灰、组件内 |
| 输入 / 切换 / 数值框 | 高 28 | 桌面最小点击 24–32 |
| 图标按钮 | 16 / 24 | |
| 分割线 | **1** | `#e5e5e6` |
| 右侧栏（可选） | **320** | 宽度预设 `板块宽度_320_340_360`；用户要报价栏 / 问答栏才加 |
| 组件库抽屉（可选） | 约 280–320 | 内用 3 级选项卡 |

完整窗才出现的铬件（默认不画）：左轨、顶栏搜索、Trade 80×28、底栏指数、最右功能栏 40。

### 5.2 间距（紧凑）

原则页边距仍「梳理中」。执行以可变密度变量为准，mode「紧凑」：

| 名称 | 紧凑 | 用在 | Figma |
| --- | --- | --- | --- |
| 4 | 4 | 标签贴着数值、图标贴着文字 | `sp_margin_xs` |
| 8 | 8 | 组内；图标贴控件 | `sp_margin_s` / `padding_s4` / `sp_page_l` |
| 12 | 12 | 组外 | `sp_margin_m` / `padding_m1` |
| 16 | 16 | 卡垫；两组分项间距；浮层 | `padding_m3` |

亲近：标签↔值 4；组内 8；瓦片无外垫（拼缝）；模块间隙靠 1px 线不是 24 空。  
同一容器只用两种间距，组内更小。  
禁止整页间距都是 8，也禁止每个模块都加 16 内边距。

### 5.3 圆角

原则页「圆角」`721:685043` 已梳理。

| 对象 | 值 |
| --- | --- |
| 表格、看板上的模块、主内容块、瓦片 | **0**（结论卡外框：8） |
| 导航选中、列表选中、输入、下拉、按钮、图标按钮 | **4** |
| 气泡、轻提示、Popover | **8** |
| 搜索、胶囊主操作 | **胶囊 100** |

### 5.4 层次

主要靠 1px 线和底色差，不用大阴影。  
表格和行情区域不用玻璃。  
浮层（对话框、侧栏、气泡）可以比主区略抬一层。

z：画布 0 · 栏 1 · 抽屉 / Popover 4 · 顶栏控件 5。

### 5.5 动效

不超过 300ms。只做透明度和位移。禁止弹跳。  
键盘操作可以没有装饰动画。

加载：不到 0.3 秒不提示；0.3–2 秒用骨架屏（按数据来源分：行情 / 基本面）；超过 2 秒写说明；超过 10 秒给重试。

派生：

- 列表选中：1px 深线框，不是大色块
- 2 级导航选中：黑胶囊，**页面只 1 个焦点**
- 3 级 / 组件内选中：浅灰底
- hover 行：浅底，不发光
- disabled：opacity 0.4
- focus：2px 线，offset 2，不可 `outline: none`
- 导航选中图标：橙或橙浅底，不新造第三色

### 5.6 信息层级（结论类模块 · 桌面）

用户要先判断「能不能据此加减仓」，不是先看一张海报仪。扫读顺序：结论强不强 → 周期是否同向 → 趋势与动量是否同向 → 价在均线哪一侧 → 分项。

正文 12。全卡只有结论词 16 加粗。字段名和小模块标 12 加粗，辅助信息 Regular。卡宽 **360**。卡垫 16。表行 28。结论词和「共识弱」成组坐进半圆碗，组内 2–4，底边贴齐左右端刻的弦，与刻度内侧空开 ≥ 8。少用分区标题，靠顺序和一条读法说话。

间距（只准 4 的倍数）：

| 档 | px | 用在 |
| --- | --- | --- |
| 缝 | 4 | 组内紧邻：数字压条、计量标签↔轨、表单元格 |
| 条 | 8 | 同组子模块：身份↔周期、仪↔构成。图内价轴行距。只留白，不画线 |
| 图字 | 16 | 图内标注行高 = 正文行高。禁止 12 行里放 12 字 |
| 组 | 24 | 大模块之间，只靠留白。卡垫仍是 16 |
| 线 | 0.5 | 表行 0.5px、卡片外框 0.5px |
| 禁 | 10 / 12 / 14 | 不准用。控件内缝 2 除外。禁止非表格分割线 |

四组：身份周期 | 结论（仪+构成）| 分项（Oscillators / Moving averages）| 读法。两组上下排列，禁止两列。组名与组结论 12 加粗，结论靠右。组条左高亮右灰。指标整宽表。

排版（层级不动，只约束怎么排）：

- 全卡左右齐栏，卡宽 360。结论仪居中辅图，宽 192。「中性」和「共识弱」成组进碗，组内 2–4。禁止拆开，禁止左右分栏来填空。
- 垂直节奏按上表。禁止整卡一个 gap。
- 同类模块同一骨架：标签行 + 轨道 + 轴注。构成是「数字压在比例条上」的一组。
- 表四列定宽，数字右齐。
- 不准为了填满或收空改扫读顺序。

| 层 | 用户问什么 | 字 | 展示 |
| --- | --- | --- | --- |
| 0 身份 | 看哪只、什么价 | 标的 12 加粗，价 / 涨跌 12 Regular | 一行：模块名弱 + 标的 + 价 / 涨跌 |
| 1 周期同向 | 日线结论能不能代表更大周期 | 12 | 周期条上置；选中墨色胶囊；涨跌色只给方向 |
| 2 结论 | 现在偏多偏空、共识够不够 | 结论 16 加粗，其余 12 Regular | 碗里一组贴齐底弦：中性 + 共识弱 · 10/26，组内 2–4 |
| 3 构成 | 票数有没有一边倒 | 字段 12 加粗，票数 Regular | 卖 / 中 / 买单独一行 + 一条比例条 |
| 4–6 分项 | Oscillators / Moving averages | 组名 / 组结论 / 指标名 12 加粗，其余 Regular | 上下两组。Oscillators Neutral、Moving averages Buy。组条左高亮右灰。禁止 Mom / Trend / Mix / 两列 |
| 7 读法 | 所以怎么办 | 12 Regular | 一行决策句；脚注 12 弱 |

禁止：正文大于 12（结论词 16 除外）；组名放大；辅助信息加粗；结论词 28+；分区标题堆「信号构成 / 分组 / 分项」；图色铺到动量条上。

生成设计稿还要避开这三条（本卡已踩过）：

1. **字和图不要贴死。** 结论词坐进碗，与刻度内侧空开 ≥ 8。禁止贴刻度。
6. **就近原则。** 「中性」和「共识弱」必须成组，禁止拆到碗里和仪下。
2. **不画无含义装饰。** 斜纹底、说不清含义的色块都不要。竖线只标均线档位和现价，禁止两端一撮装饰刻度。
3. **少用灰色块。** 周期选中用墨色胶囊突出焦点，不要浅灰底铺一排，也不要用细下划线冒充选中。轨道用 1–2px 线。灰只给弱文和 Neutral 票数，不当底。
4. **禁止负向优化。** 改完必须还能一眼读出周期焦点、结论、价在 50/200 哪一侧。看不懂或层级乱了就回退。
5. **层级优先于填满。** 可适当收留白，禁止把结论或票数塞到仪侧面来填空。
7. **组条跟组结论走，不跟子行走。** 禁止 Mix / Mom / Trend。表里 SMA50 Sell 可以和组头 Buy 并存。

---

## L6 金融组件规格

完整语义与禁止项写在 `components.md`。这里只锁标准稿里有、且本看板要用的尺寸。  
**库优先只管控件，不管图。** Tag / Button / Input / 分段：库有就用库，不准另画一套。  
半圆仪、计量条、迷你图、主图：库没有也必须画，用来降低读数成本；色 / 字 / 距仍绑 token。  
禁止用进度条 + Tag 顶替结论仪表。禁止为了「不手画」减掉参考图里的可视化。  
利率情景、估值计算器、热力图、滑块调参：本轮不画。

### 6.1 最低集

| 组件 | 要 | 用途 / 尺寸 | 禁止 |
| --- | --- | --- | --- |
| 报价头 | [x] | 代码、最新价、涨跌幅。字 Headline3/13 或 Body/12 | 做成超大展示标题 |
| 表行 / 自选行 | [x] | 表行 28；自选元件 220×46。选中细框 | 整行铺品牌主色 |
| 主图 | [x] | 高度至少 160 | 压成又扁又长（宽比高超过 8 倍） |
| 迷你走势图 | [x] | 行内辅助，高度约 48 | 当成主图 |
| 贵 / 中 / 便宜标记 | [x] | 估值结论，只读。库 Tag | 当成可点筛选标签 |
| 涨跌标记 | [x] | 只表示方向 | 和贵贱混用 |
| 延迟 / 冻结 | [x] | 标「延时」，价格可冻住 | 看起来像实时 |
| 确认框 | [x] | 买入/加仓含义、改板等不可逆 | 普通筛选也弹确认 |
| 模块空位 | [x] | 点阵上的虚线空位，可添加 | 插画空状态当装饰 |
| 骨架屏 | [x] | 按行情或基本面分开 | 整页一个转圈 |
| 空值 | [x] | 「—」 | 用邻格填洞 |
| 半圆仪 / 结论仪表 | [x] | 宽 192。单环读数。白刻只刻在读数上，长短错落，首尾隐藏。受光硬边 | 另加共识环；软阴影；白缝贯穿；圆点收尾 |
| 组计量条 | [x] | 2px。左高亮（组结论色）右灰，比例跟 Neutral / Buy，不按子行切段 | 当成主图；粗于 2px；按 RSI/MACD/SMA 色切段 |

### 6.2 通用控件（不是窗铬）

下面这些按功能需要出现，**不是每张稿的必套壳**。未勾选的铬件（轨 / 顶栏 / 底栏 / Trade）默认不画。

| 组件 | 要 | 备注 |
| --- | --- | --- |
| 按钮 | [x] | 同一屏实心主按钮最多 1 个。买入绿、卖出红。橙色只给明确的主操作 |
| 搜索 | [ ] | 功能需要才画。高 28、胶囊 100。不要为了像顶栏硬加一条 |
| 输入 | [x] | 高 28、圆角 4。库「常规单行输入框」档位 28 |
| 选项卡 | [x] | 1 级下划线 36；2 级黑胶囊 40；3 级浅灰 36。场景 Tab ≠ 组件分段 |
| 标签 | [x] | 分类筛选，不是状态。库 Tag |
| 导航 | [ ] | 只要完整窗才画 1 / 2 / 3 级。功能面不要自动加左轨 |
| 对话框 / 侧栏 / 气泡 | [x] | 打断、详情、轻量说明。侧栏 320；气泡圆角 8 |
| 轻提示 | [x] | 短暂反馈，不代替确认框 |

### 6.3 每个组件都要写的状态

默认 · 悬停 · 按下 · 选中 · 禁用 · 键盘选中 · 加载 · 错误

键盘选中必须看得见，不能去掉描边。  
悬停行用浅底，不发光。  
禁用用 opacity 0.4，不要灰到看不清。  
错误：跌色文案，不大红铺底。

填写模板见大纲 6.4；实例化时引用 L2 / L3 / L4 / L5，禁止写死色值。

---

## L7 投研页面

按这次要的页面类型画，**不要先套窗再填内容**。

| 页面 | 布局 | 备注 |
| --- | --- | --- |
| 功能面（默认） | 一块模块或一组模块，Hug 内容 | 技术评级、报价头、表、图都直接出，不要外加轨 / 顶 / 底 |
| 完整桌面窗（可选） | 轨 + 顶栏 + 主区 + 底栏 | 仅用户点名「完整窗」时装配，尺寸跟标准稿，不当默认 |
| 三栏行情 | 列表 220 \| 主表或图 \| 右侧报价 320 | 数字右齐；涨跌色只给涨跌幅 |
| 自定义看板 | 点阵画布 `#f9f9fa` 上的模块，尺寸可以不一样 | 模块白底、直角、1px 线。瓦片头：代码/名称 + 钉/关。禁止 8 张一样高的卡片横排 |
| 图表 | 白底、细轴、少系列 | 主图高度至少 160；最多 2 条面积 + 3 条线 |
| 基本面记分卡 | 结论在前，依据可展开 | 同行中位行必须在 |
| 空状态 | 对象 + 下一步，无插画也可以 | 不要铺装饰卡 |
| 加载 / 部分失败 | 保留模块框 | 单源失败不影响其他源 |
| 无实时权限 | 报价冻结并标延时 | 基本面仍可读 |
| 浮层 | 对话框 / 侧栏 / 气泡 | 确认框写清将发生什么 |
| 设置 / 表单 | 可比主台松一档 | 输入高仍 28 |
| 问答右栏 | 场景 AI：右 320，叠在行情旁 | 12px 文字行，不是彩色推荐卡，不是深色聊天气泡墙。结论必须带「模块 + 字段 + 数」 |
| AI 应用 | 左约 220 会话列表，中空态，底浅灰输入条 | 独立页，不是本看板默认。模块 B 对齐场景 AI 右栏 |

不写：宣传首页、摄影、页脚、品牌故事带。

---

## L8 必须做与禁止做

细则在 `craft.md`。这里只锁和 token / 框有关的硬规则。

### 8.1 必须做

- 先画这次要的功能内容；不要为了对齐标准稿自动加壳
- 生成时先读「标准卡」。遵守出稿纪律：字与图留得出气；就近原则；间距只准 4 / 8 / 16 / 24；层级靠留白，只有表行用 0.5px 线；图服从字；组条跟组结论；改动从用户视角验收，禁止负向优化
- 结论类评级必须出仪表或等价图，不准只丢 Tag + 数字
- 颜色、字体、间距全部引用设计变量或库样式
- 同一屏只有一个主任务
- 中文能换行，或明确显示省略号，禁止被框裁掉
- 涨跌色全产品一致
- 没有对比基准的数字不上颜色
- 延时不能长得像实时
- AI 结论必须带来源字段

### 8.2 禁止做

- 紫蓝渐变、行情和表格上铺玻璃、英文大写装饰小标题
- 中文界面用 Inter / Roboto / 私设 PingFang
- 多张一样大的卡片铺满一屏
- 报价或表头做成 28px 以上的展示标题
- 用框架强调色表示涨跌、贵贱或图表线条
- 买入/卖出按钮用橙色（买入绿、卖出红）
- 弹跳、装饰粒子
- 第二个主色、大阴影、全部做成两端全圆
- 绝对值不给参照
- 数字精度过高
- 只给当期、没有时间或趋势
- 从空白 Frame 重画桌面终稿
- 用户只要一个功能，却套上左轨 / 顶栏 / 底栏 / Trade
- 字和图标贴死、灰色块铺底、说不清含义的装饰
- 图内价轴行高小于 16，或密轴不减刻度导致字贴字
- 为了少灰块把选中收成看不清的细线；为了多竖线把均线轴画成看不懂的刻度簇
- 为了填满把结论或票数塞到仪侧面，打乱「仪 → 结论 → 构成」的扫读
- 间距用 10 / 12 / 14，或整卡一个 gap 导致组切不开
- 在表格以外的模块之间画分割线
- 把结论仪做成卡内最大色块 / 宽过 220，压过结论词

### 8.3 变窄或变宽

窄：减少栏和模块，不把中文收到 9px。  
宽：先加主区，再增加侧栏（320 / 340 / 360）。原则页已有 1260 / 1440 / 1760 样张。  
桌面控件 24–32px。手指点击规则不作为本大纲默认。

### 8.4 读得清

正文和背景对比度至少 4.5:1。  
键盘选中看得见。  
图标按钮有名字。

---

## L9 对应到 Figma

没有本层，设计稿算还没开始。代码侧映射见 `library-map.md`。

### 9.1 设计变量对应表

| 用途 | Figma 对象 | 编号 / 集合 | 浅色 + 紧凑 |
| --- | --- | --- | --- |
| 画布底 | 颜色变量 | `ct_bg_frame` · Color Token | `#f9f9fa` · 牛牛-白肤 `41651:1` |
| 表 / 模块底 | 颜色变量 | `ct_bg_primary` | `#ffffff` |
| 导航底 | 颜色变量 | 新版颜色「导航背景色」`185:61937` | `#eff2f6` |
| 主文字 / 次文字 | 颜色变量 + 文字样式 | `ct_text_h1` / `h2` / `h3` + Body/Headline | `#171718` / `#646466` / `#98989a` |
| 线 | 颜色变量 | `ct_line_list` | `#e5e5e6` · 1px |
| 涨 / 跌 | 颜色变量 | `ct_text_trends_green` / `red` | `#00a870` / `#f03a55` |
| 框架强调色 | 颜色变量 | `ct_text_highlight` / `ct_bg_highlight` | `#f36f12` |
| 间距 | 密度变量 | `156:189077` · `sp_margin_*` / `padding_*` | 4 / 8 / 12 / 16 |
| 表行 | 组件实例 | 列表 · 表格 `be5228f8…` | 行高 28 |
| 自选行 | 组件实例 | 自选列表元件 | 220×46 |
| 报价头 | 组件实例 | 对齐终稿 12–14，不手画英雄价 | Headline3/13 |
| 输入 / 按钮 | 组件实例 | Input `6961a1d4…` 档位 28；Button `6b061613…` | 高 28 / 24 |
| 状态标记 / 标签 | 组件实例 | Tag `d699baf0…` | 禁止另外手画一套 |
| 分段 | 组件实例 | 三级筛选选项卡 `9ff460ff…` | 固有宽约 520，槽不够就减项 |

对应方法：

1. 颜色绑到变量上（`importVariableByKeyAsync` + `setBoundVariableForPaint`）
2. 文字挂库里的文字样式（`importStyleByKeyAsync`）
3. 绑定失败：建同名本地变量，禁止写死色值
4. 库缺的是 Tag / Button / Input / 分段：记 `gaps.log`，不准另画一套控件。库缺的是图 / 仪 / 计量 / 面积：自研几何，仍绑 token，不准减功能
5. 库缺深色组件：整页浅色，禁止深浅混用

代码里只引用这些角色对应的 CSS 变量（`library-map.md`）：

| 角色 | CSS |
| --- | --- |
| 画布 / 瓦片 / 浅底 | `--color-bg` `--color-surface` `--color-rail`（浅底角色，不是必画左轨） |
| 字 1/2/3 / 线 | `--color-ink` `--color-mute` `--color-faint` `--color-line` |
| 涨 / 跌 / 强调 | `--color-up` `--color-down` `--color-accent` |
| 间距 4/8/12/16 | `--space-1` `--space-2` `--space-3` `--space-4` |
| 圆角 | `--radius-pill` 100 · `--radius-control` 4 · `--radius-tile` 0 · `--radius-pop` 8 |
| 完整窗（可选，默认不用） | `--rail` 48 · `--titlebar` 44 · `--statusbar` 32 · `--ask` 320 |

### 9.2 出现即不能验收通过

| 问题 | 含义 |
| --- | --- |
| 变量没绑上 | 未绑定的填充、间距、文字样式超过 20% |
| 文字样式缺失 | 文字没挂库样式 |
| 有库不用 | 库里有对应组件却手画 |
| 两套控件叠在一起 | 手画和库组件表示同一用途 |
| 文字被框裁掉 / 容器装不下 | 字或模块被切掉 |
| 组件比格子大 | 组件本身大于放它的格子 |
| 间距没有主次 | 同一容器超过两种间距 |
| 图读不清 | 主图过扁、线条过多 |
| 业务含义画错 | 品牌色当涨跌、延时看起来像实时、无参照却上色 |
| 在已经装不下的结构上换颜色 | 结构坏了还继续换肤 |

### 9.3 验收怎么做

1. 整页截图
2. 每个模块单独截图
3. 检查变量有没有绑上
4. 失败只改 1 个模块，禁止整页重画来「顺便修」

### 9.4 本轮缺口（不 invent）

- 延迟 / 警告无独立 token
- 图表序列色未从标准稿抽出
- 边距原则页仍「梳理中」，先跟紧凑 4 / 8 / 12
- 导航层级原则页仍「正在跑」，结构按 1 下划线 / 2 黑胶囊 / 3 浅灰用
- `MooPC行情` 未订阅
- 本对话未再挂上 Figma MCP；色值与框来自 2026-09-18 实读 + 仍在磁盘的节点 dump。变量 **key** 未全表回填，绑定时按名称 `importVariableByKeyAsync`，失败建同名本地变量

---

## 和现有文件的关系

| 本文件层 | 还写入 |
| --- | --- |
| L0–L1 | 本文件意图；`visual-spec.md` |
| L2 | `domain.md` |
| L3–L5 | 本文件（色 / 字 / 距 / 框） |
| L6 | `components.md` |
| L7 | `template.md` |
| L8 | `craft.md` |
| L9 | `library-map.md` + `gaps.log` |

功能、流程、空态和权限仍走 `spec.md`，不塞进设计系统。  
后续生图走项目 skill `.cursor/skills/gen-from-design` 与 rule `.cursor/rules/gen-from-design.mdc`。
