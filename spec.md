# AI 自定义金融数据看板 · 交互设计 Spec

评审模式：`prototype`  
数据状态：用户已确认 `cards.md` 无误（2026-09-18）  
页面类型：Data Dashboard（主）+ Conversational（独立问答模块）  
产品名（工作）：自定义投研看板  
IA 锁定：**双模块**（金融决策看板 ∥ AI 问答）。B 对话编排已废弃：问答不得嵌进画布，OmniBox 不得横跨两区。

## L1 定位与意图

- 一句话定义：给基本面投资者一张**自己拼出来的金融数据看板**，并能用问答只针对这张板上的模块与单元格做解释、对比、假设拆解。看板是工作台，AI 是绑在这张板上的分析员，不是第二个仪表盘。
- 目标用户：会按自己的研究框架看估值 / 质量 / 事件的人（不是 K 线交易员，也不是只想看一篇 AI 研报的浏览用户）。
- 场景清单：
  1. **空白起步**：一句话生成初始板（例：「苹果 估值分位 + 同行 + 近端催化剂」）
  2. **改板**：增删模块、换标的、改时间窗（1Y/3Y/5Y/10Y）、保存为「我的估值板」
  3. **读板**：开盘前 3 分钟扫自己的板，判断贵不贵、质量在不在、近端会不会打脸
  4. **追问**：点某一列 / 某一模块问「相对 GOOGL 的边际」「回购停了分位怎么变」
  5. **补洞**：问到板上没有的指标时，先提案加模块，用户确认后再出数
- 非目标：
  - 不下单、不改账户、不做完整选股器 / 筛选器结果页
  - 不做 K 线主图、画线、技术指标四件套、Pine 脚本
  - 不复制 TradingView Remix 的「从零拖满一墙卡片」营销流程帧，不把 builder 当首页演出
  - 不让模型自由生成任意 HTML/CSS 页面；模块必须来自组件目录
  - 不把 AI 做成第二套数（禁止回答里出现板上没有、且用户未确认新增的指标值）
  - 不做组合下单、持仓编辑、资金划转
  - 不交付路径页：空板起步、缺指标确认流、编辑态演示帧；只交付最终生成面板
- 行为边界：
  - 始终：涨跌色全局稳定；价格与倍数用等宽数字；分位与同行中位数可同屏扫读（模块在板上时）；AI 每条结论带 `模块 + 字段 + 数值` 依据；自定义不把模块做成等权装饰墙
  - 询问后：增删模块、换标的、给出买入/加仓含义时，必须用确认卡写清将发生什么；用户确认前不改板
  - 永不：无依据的「强烈推荐」；默认明文展示持仓金额；把延迟数据说成实时；用品牌色冒充涨跌

## L2 信息架构

整页只有 **两个一等模块**，视觉上必须是两块独立容器（各自标题、描边、内边距），禁止问答气泡、确认卡、OmniBox 出现在金融看板内部。

1. **模块 A · 基本面决策看板**（主，约 72%）  
   卡片功能与数据以 `cards.md` 为唯一源（视觉不跟 Remix）：
   1. 顶部摘要 SUMMARY
   2. 股票概览 Quote
   3. 分析师目标价 12M
   4. 基本面记分卡
   5. 估值倍数（含 P/B，不是 P/FCF）
   6. 股息健康
   7. 占位卡 Add widget
   8. 收入与盈利趋势（底栏）
2. **模块 B · AI 问答**（约 28%）  
   只解释模块 A 已有列；OmniBox、对话、依据卡 **全部住在本模块内**。
2. **模块 B · AI 问答**（约 28%）  
   只解释模块 A 已有列；OmniBox、对话、依据卡、改板提案 **全部住在本模块内**。

区域边界：

- 两模块不得互相嵌套；中间用间隙分开，不共用一张大卡
- 模块 A 是判断主区；模块 B 不另开一套指标墙
- 改板提案若出现，只在模块 B；确认后只改模块 A 的数据卡，不把聊天画进 A
- 目录只在编辑态，且属于模块 A 的工具层，不进问答

内容生长：

- 模块 A 优先在已有决策卡内加行 / 加列 / 改时间窗，不横向再铺一张「装饰卡」
- 模块 A 决策卡上限按上述 6 步，不堆到 9 宫格
- 模块 B 过程可滚动；高度跟随模块 A，不撑破一屏决策

## L3 核心链路

- 状态清单：
  - 空板
  - 编排中（AI 提案 / 用户从目录加）
  - 数据就绪
  - 部分字段延迟
  - 单模块失败
  - 追问中
  - 待确认改板
  - 无实时行情权限（报价冻结，基本面仍可读）
- 主链路：进入 → 只扫模块 A（摘要 → 报价/目标/记分卡 → 估值/股息 → 趋势）→ 需要时进入模块 B 追问 → 离开
- 分支：
  - 点单元格 → OmniBox 带上该字段上下文
  - 问到缺失指标 → FlexCard 提案「添加某某模块」，确认后加载，再回答
  - 编辑态拖拽排序 / 移除；阅读态隐藏拖拽柄
  - 换标的 → 全板绑定同一主标的（模块可声明例外，如同行表）

## L4 组件功能

| 组件 | 定位 | 默认 | 悬停 | 加载 | 禁用 | 错误 |
| --- | --- | --- | --- | --- | --- | --- |
| 对象条 | 现在怎样 | 实底；涨跌 Badge | 时延 tooltip | 行情骨架 | 无权限时涨跌灰置 | 行情源失败文案 |
| 模块（Widget） | 一块可追问的证据 | 实底表/分位条 | 单元格可点 | 表内骨架 | 编辑锁时可关 | 保留模块框 + 失败原因 |
| 分位条 | 贵不贵 | 标签+倍数+分位+中位 | 时间窗切换 | 轨道骨架 | 无历史则空值行 | 分位源失败 |
| 同行表 | 相对谁 | 主标的行强调；中位行始终在 | 行高亮 | 行骨架 | — | 缺某 peer 不删表 |
| OmniBox | 意图入口 | 占位「问这张板 / 改这张板」 | 聚焦展开对象+范围 | 发送中 | 无权限只读问 | 发送失败可重试 |
| 依据卡 FlexCard | 过程协作 | 引用模块字段 | 可点回画布定位 | 生成中标明「引用中」 | 高权限动作未确认 | 无依据则不准出结论 |
| 改板确认卡 | 打断 | 将增/删哪个模块 | — | — | 确认前板不变 | 用户取消则回滚提案 |
| 模块目录 | 合法积木 | 按决策用途分组，不是字母墙 | 预览用途一句话 | — | 已达上限的项禁用 | — |

危险动作（买入/加仓含义）：不放主路径；若 Ask 触及，必须二次确认文案并写假设。  
敏感字段（持仓金额）：默认不展示；若用户主动要看，需显式展开。

## L5 边界条件

- 空态：空板保留对象条 + OmniBox + 3 条起步示例，不铺示例卡片墙；某模块无数据时**保留模块**，行内显示「—」和原因（未覆盖 / 延迟 / 无权限）
- 加载：<300ms 不展示；300ms–2s 只在该模块表内骨架；>2s 「基本面源延迟 / 行情源延迟」分源说明；>10s 模块级重试，不整页刷新
- 错误：单模块失败不影响其他模块；错误文案写清是估值源还是行情源；AI 不得用相邻模块的数填洞
- 权限：无实时行情则冻结对象条报价并标延时，Ask 仍可基于已加载基本面；无某类基本面权限则目录中对应模块锁定，说明升级原因，不静默藏数据

## L6 验收标准

- Given 空板，When 用户说「AAPL 估值分位、同行、催化剂」，Then 先出确认卡列出将添加的模块，确认后画布出现这三块且数字可扫读
- Given 板上已有 Fwd P/E 与同行，When 用户问「相对 GOOGL 的边际」，Then Ask 引用板上 Fwd P/E 与 GOOGL 行，不另造一套倍数
- Given 用户问板上没有的毛利率趋势，When AI 响应，Then 只提案「添加质量/利润率模块」，确认前不出现具体毛利率数字
- Given 估值源超时 >10s，When 其他模块已就绪，Then 仅估值模块显示失败+重试，报价与同行仍可用
- 设计完成定义：一眼能分出「看板 / 问答」两块；模块 A 3 分钟能答贵不贵、质量、催化剂；模块 B 引用 A 的单元格；无等权卡片墙；无玻璃铺满数据区；至少一处 L5
