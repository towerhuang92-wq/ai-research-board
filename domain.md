# domain.md

业务对象：看板 Board、模块 Widget、字段 Field、追问 Turn、改板提案 Proposal。

## 对象 × 状态 → 表达

| 对象 | 中性 | 进行中 | 成功 | 需要恢复 |
| --- | --- | --- | --- | --- |
| Board | 阅读态，模块实底 | 编排中 / 待确认改板 | 提案已钉上，画布多一块 | 空板：对象条+OmniBox+示例句，不铺卡墙 |
| Widget | 数据就绪 | 表内骨架 | 可点单元格追问 | 保留框；「—」+ 未覆盖/延迟/失败 |
| Field | 有值、等宽 | 该格骨架 | — | 「—」不得用邻格填洞 |
| Quote | 实底价格+涨跌 Badge | 行情骨架 | — | 无实时：冻结并标「延时」 |
| Turn | — | 「引用中」 | 依据卡含模块+字段+数 | 无依据不准出结论 |
| Proposal | — | 确认卡列出将增删的模块 | 确认后才改画布 | 取消则提案消失，板不变 |

## 涨跌与贵贱

- 涨跌色只表示价格/变动方向，token：`color/up` `color/down`。禁止 primary 蓝冒充。
- 估值分位按「贵/便宜」：>75 贵（down/warn），25–75 中性，<25 便宜（up）。FCF Yield 等越低越贵的指标，按贵贱语义上色，不按分位数字机械套绿。
- 同行中位行必须存在；缺 peer 删行不删表。

## 危险操作与敏感字段

- 买入/加仓含义：非主路径；出现则二次确认，文案写假设（示例仓须标明「演示」）。
- 持仓金额默认不展示。

## AI 契约（A2UI）

Agent 只声明要表达什么（intent / 引用字段 / 提案模块 id）。客户端用目录组件渲染。禁止在消息里指定颜色圆角字体。  
问到板上没有的指标：只出 Proposal，确认前不出现该指标数字。
