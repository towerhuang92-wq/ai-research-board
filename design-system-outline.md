# design.md 大纲 · 金融产品界面

适用：桌面投研工作台（数据看板 + 工作台）。不写品牌宣传网站。  
填完后写入 `design.md`，并对应到 Figma 的颜色变量、文字样式、组件。  
产品现行已填样式仍以根目录 `design.md` 为准。本文件是空大纲。

交易按钮可以出现在完整桌面窗上，**业务不做下单**。默认生图只出功能面，不套左轨 / 顶栏 / 底栏。利率情景、估值计算器先当扩展，不进最低集。

---

## 填写顺序

```text
L0 先确认界面类型
  → L1 工作台气质
  → L2 业务含义色与数字规则
  → L3 中性底 + 框架强调色
  → L4 字与数
  → L5 疏密 / 表与模块（壳不锁）
  → L6 金融组件规格
  → L7 投研页面
  → L8 必须做与禁止做
  → L9 对应到 Figma
```

L0 / L1 没确认，不准填色值，不准开 Figma。  
L2 没填完，不准写 L3。涨跌和数字规则先于品牌主色。  
L9 没写成对应表，设计稿算还没开始。

---

## L0 先确认界面类型

未确认前，本大纲不驱动任何画板。

- [ ] 主界面：工作台 + 数据看板（可附右侧问答栏）
- [ ] 不做：品牌宣传网站、首页大图、摄影、页脚
- [ ] 气质一句话已确认
- [ ] 3 个「不像」已确认
- [ ] 疏密：紧凑（金融桌面默认）
- [ ] 中文优先：是
- [ ] 明暗：浅色主题（设计库缺深色组件时，整页不要深色）
- [ ] 品牌主色只给导航选中、主操作，不进涨跌和贵贱。没有壳就不画 Trade

**停下来等确认。**

---

## L1 工作台气质

只写判断，不写色值。

| 字段 | 填写 | 约束 |
| --- | --- | --- |
| 一句话 | | 界面类型 + 底色 + 疏密 + 主色用在哪。例：「浅色高密度终端。数字密、标题弱。橙色只给交易按钮和导航选中。」 |
| 像 | 1.  2.  3. | 点名真实产品，不要写「现代 / 高级」 |
| 不像 | 1.  2.  3. | 必须包含：不像宣传海报；不像多张一样大的卡片铺满一屏。建议再写：不像深色聊天应用 |
| 界面靠什么传递信息 | 数字 / 表格 / 图表 | 不靠照片、不靠插画、不靠超大标题 |
| 圆角规则 | 表格和模块直角或小圆角；只有搜索和交易按钮可以两端全圆 | |
| 一年后检查 | | 还是同一张投研桌，只改了哪一项（不要整页换皮） |

标志性视觉：无。靠疏密、等宽数字和 1px 线识别，不上渐变、玻璃或海报大字。

---

## L2 业务含义色与数字规则

本层先于品牌主色。颜色表示含义，不是装饰。

### 2.1 三种颜色各管各的

| 种类 | 表示什么 | 不准表示什么 |
| --- | --- | --- |
| 涨 / 跌 | 价格和变动方向 | 贵贱、成功失败、品牌装饰 |
| 贵 / 中 / 便宜 | 估值相对历史或同行（例如分位 >75 贵，<25 便宜） | 价格涨了就标绿。越低越贵的指标按「贵贱」上色，不按数字大小机械套绿 |
| 延迟 / 冻结 / 无权限 | 数据不可用或不是实时 | 看起来像实时行情 |
| 成功 / 警告 / 危险 | 操作有没有做成 | 涨跌 |
| 框架强调色 | 导航选中、交易按钮 | 盈亏、涨跌幅、估值结论、图表线条 |

全产品涨跌色一致，中途不换含义。没有同行或历史对比时，数字不要上颜色。

### 2.2 数字怎么写

| 规则 | 填写 / 约定 |
| --- | --- |
| 字体 | 价格、涨跌幅、倍数用等宽数字 |
| 对齐 | 标签左、数字右 |
| 精度 | 倍数保留 1 位（如 23.5x）。价格按市场惯例。不要写 23.4567x |
| 空值 | 用「—」。不拿旁边格子的数来填 |
| 预测 vs 历史 | 必须能区分（虚线 / 透明度），不能画成同一种实线 |
| 参照 | 分位、同行中位必须能同屏扫到。缺同行就删这一行，不删整张表 |
| 延迟 | 必须标「延时」或冻结，不能长得像实时 |

### 2.3 三层信息

1. 默认：结论（低估 / 合理 / 高估；健康 / 警告）
2. 点开：依据（P/E 15x，历史 20% 分位）
3. 再进：原始表或公式

禁止把 50 个指标一次性铺平。

---

## L3 中性底 + 框架强调色

大约 90% 的像素是灰、白和线。强调色很少用。  
组件只引用有含义的名字，不直接涂基础色值。

### 3.1 基础色值（不直接涂在组件上）

| 名称 | 色值 | 备注 |
| --- | --- | --- |
| 涨 | | 只给方向 |
| 跌 | | 只给方向 |
| 贵 / 便宜 | | 可与跌 / 涨共用，但规则走 L2，不另开一套彩虹 |
| 延迟 / 警告 | | |
| 灰 50 / 100 / 200 | | 画布、浅底、线 |
| 字 1 / 2 / 3 | | 主 / 次 / 弱 |
| 框架强调色 | | 只给导航选中、交易按钮。同色相派生按下和悬停，不新开一种色 |
| 买入底 / 卖出底 | | 绿实底 / 红实底，不是框架强调色 |

### 3.2 有含义的颜色名（对应 Figma 颜色变量）

| 名称 | 引用上面的基础色 | 浅色 | Figma 变量 |
| --- | --- | --- | --- |
| 画布底 | | | |
| 卡片 / 表底 | | | |
| 导航底 | | | |
| 主文字 / 次文字 / 弱文字 | | | |
| 分割线 | | | |
| 选中描边 | | | |
| 涨 / 跌 | | | |
| 框架强调色 / 强调色上的字 | | | |
| 图表色 1…n | | | 不得和涨跌相同 |

正文和背景对比度至少 4.5:1。更浅的灰只给坐标轴和脚注。  
同一屏里，框架强调色的实心底最多 1 处。涨跌色不算品牌色。

### 3.3 浅色 / 紧凑

| 切换 | 何时用 | 缺设计库时 |
| --- | --- | --- |
| 浅色 | 默认 | |
| 深色 | 本大纲默认不上 | 缺深色组件则整页浅色，禁止深浅混用 |
| 紧凑 | 桌面终端、表格 | |
| 常规 | 设置页、空状态，可松一档 | |

---

## L4 字与数

每一档必须能变成一条 Figma 文字样式。画稿时禁止手写字号。  
软件界面不用超大标题（28px 以上的展示字）。

| 样式名 | 字体 | 字号 | 行高 | 字重 | 数字等宽 | Figma 文字样式 |
| --- | --- | --- | --- | --- | --- | --- |
| 栏目标题 | | 约 14 | | Bold | 否 | |
| 组件头 | | 约 12–13 | | | 否 | |
| 正文 / 表 | | 12 | | Regular | 表内数字要 | |
| 强调数字 | | 12 | | Bold | 要 | |
| 注释 / 坐标轴 | | 10–12 | | Regular | 否 | |
| 等宽 / 代码 | | 12 | | | 要 | |

规则：

- 中文模块名、表行至少 12px；坐标轴至少 10px。禁止为了塞进一屏收到 9px。
- 字重最多 3 档，工作台通常是 Regular + Bold。
- 中文界面必须写中文字体。禁止把 Inter / Roboto / SF 当中文默认。
- 报价头用 12–14，不要做成海报大字。

---

## L5 疏密 / 表与模块（壳不锁）

### 5.1 画板与模块，不锁窗口铬件

默认只出功能面。左轨 / 顶栏 / 底栏 / Trade 不是必套。等大卡墙仍然失败，原因是信息结构，不是「没套壳」。  
只有用户明确要完整桌面窗时，才填下面铬件尺寸。

| 区 | 尺寸 | 备注 |
| --- | --- | --- |
| 基准画板 | | 按内容 Hug 或用户指定。不要为了对齐标准稿锁 1440×900 |
| 表行高 | | |
| 分割线 | 1 | |
| 右侧栏（可选） | | 用户要报价或问答才加，约 320 |
| 完整窗铬件（可选） | | 轨 / 顶栏 / 底栏。默认不填、不画 |

窗口变宽时：先保证主区，再增加右侧栏。默认右侧最多 2 栏。优先顺序：主区 > 报价 > 交易入口 > 功能。

### 5.2 间距（紧凑）

| 名称 | 紧凑 | 用在 |
| --- | --- | --- |
| 4 | 4 | 标签贴着数值、图标贴着文字 |
| 8 | 8 | 组内 |
| 12 | 12 | 组外（按产品选） |
| 16 | 16 | 只给浮层，不给看板上的模块 |

同一容器只用两种间距，组内更小。模块之间靠 1px 线分开，不靠 16–24 的大空白。  
禁止整页间距都是 8，也禁止每个模块都加 16 内边距。

### 5.3 圆角

| 对象 | 值 |
| --- | --- |
| 表格、看板上的模块、主内容块 | 0 |
| 导航选中、列表选中、输入、按钮、图标按钮 | 4 |
| 气泡、轻提示 | 8 |
| 搜索、交易按钮 | 可以两端全圆 |

### 5.4 层次

主要靠 1px 线和底色差，不用大阴影。  
表格和行情区域不用玻璃。  
浮层（对话框、侧栏、气泡）可以比主区略抬一层。

### 5.5 动效

不超过 300ms。只做透明度和位移。禁止弹跳。  
键盘操作可以没有装饰动画。

加载：不到 0.3 秒不提示；0.3–2 秒用骨架屏（按数据来源分：行情 / 基本面）；超过 2 秒写说明；超过 10 秒给重试。

---

## L6 金融组件规格

每个组件写：用途、类型、状态、尺寸、禁止事项。  
未勾选的 **控件** 不准另外手画一套。图 / 仪 / 计量不在此列：库没有也必须画，色距仍绑 token。

### 6.1 最低集（必须有）

| 组件 | 要 | 用途 | 禁止 |
| --- | --- | --- | --- |
| 报价头 | [ ] | 代码、最新价、涨跌幅 | 做成超大展示标题 |
| 表行 / 自选行 | [ ] | 扫读；选中用细框，不用大色块 | 整行铺品牌主色 |
| 主图 | [ ] | 高度至少 160 | 压成又扁又长（宽比高超过 8 倍） |
| 迷你走势图 | [ ] | 行内辅助，高度约 48 | 当成主图 |
| 贵 / 中 / 便宜标记 | [ ] | 估值结论，只读 | 当成可点筛选标签 |
| 涨跌标记 | [ ] | 只表示方向 | 和贵贱混用 |
| 延迟 / 冻结 | [ ] | 标「延时」，价格可冻住 | 看起来像实时 |
| 确认框 | [ ] | 买入/加仓含义、改板等不可逆 | 普通筛选也弹确认 |
| 模块空位 | [ ] | 虚线空位，可添加 | 插画空状态当装饰 |
| 骨架屏 | [ ] | 按行情或基本面分开 | 整页一个转圈 |
| 空值 | [ ] | 「—」 | 用邻格填洞 |
| 半圆仪 / 结论仪表 | [ ] | 直径 ≥ 120。指针指向综合结论 | 用 Tag + 数字顶替 |

### 6.2 通用控件（不是窗铬）

按功能需要勾选。未勾选的铬件（轨 / 顶栏 / 底栏 / Trade）默认不画。

| 组件 | 要 | 备注 |
| --- | --- | --- |
| 按钮 | [ ] | 同一屏实心主按钮最多 1 个。橙色只给明确的主操作 |
| 搜索 | [ ] | 功能需要才画。可以两端全圆。不要为了像顶栏硬加 |
| 输入 | [ ] | |
| 选项卡 | [ ] | 同级视图切换。场景选项卡 ≠ 组件内分段，禁止混级 |
| 标签 | [ ] | 分类筛选，不是状态 |
| 导航 | [ ] | 只要完整窗才画。功能面不要自动加左轨 |
| 对话框 / 侧栏 / 气泡 | [ ] | 打断、详情、轻量说明 |
| 轻提示 | [ ] | 短暂反馈，不代替确认框 |

### 6.3 每个组件都要写的状态

默认 · 悬停 · 按下 · 选中 · 禁用 · 键盘选中 · 加载 · 错误

键盘选中必须看得见，不能去掉描边。  
悬停行用浅底，不发光。  
禁用用透明度或专用色，不要灰到看不清。

### 6.4 填写模板（每个组件复制一块）

```text
组件：
用途：
类型：
尺寸：高度 / 内边距 / 最小点击（桌面 24–32）
圆角：引用 L5
颜色：引用 L2 / L3，禁止写死色值
文字：引用 L4 的文字样式
状态：
Figma：组件名 + 类型 / 状态 / 尺寸
禁止：
```

### 6.5 扩展（先不进最低集）

利率情景、估值计算器、热力图、滑块调参：以后要做再补规格，本轮不画。

---

## L7 投研页面

每张页面写：布局、疏密、主色用多少、禁止事项。

| 页面 | 布局 | 备注 |
| --- | --- | --- |
| 功能面（默认） | 一块模块或一组模块 | 不要先套窗再填内容 |
| 完整桌面窗（可选） | 导航 + 顶栏 + 主区 + 侧栏 + 底栏 | 仅用户点名时装配 |
| 三栏行情 | 列表 \| 主表或图 \| 右侧报价 | 数字右齐；涨跌色只给涨跌幅 |
| 自定义看板 | 点阵画布上的模块，尺寸可以不一样 | 模块白底、直角、1px 线。禁止 8 张一样高的卡片横排 |
| 图表 | 白底、细轴、少系列 | 主图高度至少 160；最多 2 条面积 + 3 条线 |
| 基本面记分卡 | 结论在前，依据可展开 | 同行中位行必须在 |
| 空状态 | 对象 + 下一步，无插画也可以 | 不要铺装饰卡 |
| 加载 / 部分失败 | 保留模块框 | 单源失败不影响其他源 |
| 无实时权限 | 报价冻结并标延时 | 基本面仍可读 |
| 浮层 | 对话框 / 侧栏 / 气泡 | 确认框写清将发生什么 |
| 设置 / 表单 | 可比主台松一档 | |
| 问答右栏（可选） | 约 320 宽，叠在行情旁 | 建议是 12px 文字行，不是彩色推荐卡，不是深色聊天气泡墙。结论必须带「模块 + 字段 + 数」 |

不写：宣传首页、摄影、页脚、品牌故事带。

---

## L8 必须做与禁止做

### 8.1 必须做

- 先画这次要的功能内容；不要为了对齐标准稿自动加壳
- 颜色、字体、间距全部引用设计变量或库样式
- 同一屏只有一个主任务
- 中文能换行，或明确显示省略号，禁止被框裁掉
- 涨跌色全产品一致
- 没有对比基准的数字不上颜色
- 延时不能长得像实时
- AI 结论必须带来源字段

### 8.2 禁止做

- 紫蓝渐变、行情和表格上铺玻璃、英文大写装饰小标题
- 中文界面用 Inter / Roboto
- 多张一样大的卡片铺满一屏
- 报价或表头做成 28px 以上的展示标题
- 用框架强调色表示涨跌、贵贱或图表线条
- 买入/卖出按钮用橙色（买入绿、卖出红）
- 弹跳、装饰粒子
- 第二个主色、大阴影、全部做成两端全圆
- 绝对值不给参照（只写「ROIC 12%」却看不出好坏）
- 数字精度过高
- 只给当期、没有时间或趋势
- 用户只要一个功能，却套上左轨 / 顶栏 / 底栏 / Trade

### 8.3 变窄或变宽

窄：减少栏和模块，不把中文收到 9px。  
宽：先加主区，再增加侧栏。  
桌面控件 24–32px。手指点击规则不作为本大纲默认。

### 8.4 读得清

正文和背景对比度至少 4.5:1。  
键盘选中看得见。  
图标按钮有名字。

---

## L9 对应到 Figma

没有本层，设计稿算还没开始。

### 9.1 设计变量对应表（画稿前必须有）

| 用途 | Figma 对象 | 编号 / 集合 | 浅色 + 紧凑 |
| --- | --- | --- | --- |
| 画布底 | 颜色变量 | | |
| 表 / 模块底 | 颜色变量 | | |
| 主文字 / 次文字 | 颜色变量 + 文字样式 | | |
| 线 | 颜色变量 | | |
| 涨 / 跌 | 颜色变量 | | |
| 框架强调色 | 颜色变量 | | |
| 表行 | 组件实例 | | 尺寸 = 紧凑 |
| 报价头 | 组件实例 | | |
| 输入 / 按钮 | 组件实例 | | |
| 状态标记 / 标签 | 组件实例 | | 禁止另外手画一套 |

对应方法：

1. 颜色绑到变量上
2. 文字挂库里的文字样式
3. 绑定失败：建同名本地变量，禁止写死色值
4. 库缺的是 Tag / Button / Input：记缺口，不准另画一套控件。库缺的是图 / 仪 / 计量：自研几何，仍绑 token，不准减功能
5. 库缺深色组件：整页浅色，禁止深浅混用

### 9.2 出现即不能验收通过

| 问题 | 含义 |
| --- | --- |
| 变量没绑上 | 未绑定的填充、间距、文字样式超过 20% |
| 文字样式缺失 | 文字没挂库样式 |
| 有库不用 | 库里有对应组件却手画 |
| 两套控件叠在一起 | 手画和库组件表示同一用途 |
| 文字被框裁掉 / 容器装不下 | 字或模块被切掉 |
| 组件比格子大 | 组件本身大于放它的格子 |
| 间距没有主次 | 同一容器超过两种间距 |
| 图读不清 | 主图过扁、线条过多 |
| 业务含义画错 | 品牌色当涨跌、延时看起来像实时、无参照却上色 |
| 在已经装不下的结构上换颜色 | 结构坏了还继续换肤 |

### 9.3 验收怎么做

1. 整页截图
2. 每个模块单独截图
3. 检查变量有没有绑上
4. 失败只改 1 个模块，禁止整页重画来「顺便修」

---

## 和现有文件的关系

| 本大纲 | 写入 |
| --- | --- |
| L0–L1 | `design.md` 意图 |
| L2 | `domain.md` + `design.md` 用途 |
| L3–L5 | `design.md` 颜色、字、间距 |
| L6 | `components.md` |
| L7 | `design.md` 常见页面 + `template.md` |
| L8 | `craft.md` |
| L9 | 设计变量对应表 + 缺口清单 |

功能、流程、空态和权限仍走 `spec.md`，不塞进设计系统。
